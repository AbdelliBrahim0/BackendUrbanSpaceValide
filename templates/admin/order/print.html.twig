<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Commande #{{ order.id }} - Facture</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #eee;
            padding-bottom: 20px;
        }
        .header h1 {
            color: #2c3e50;
            margin: 0;
        }
        .info-section {
            display: flex;
            justify-content: space-between;
            margin-bottom: 30px;
        }
        .info-box {
            flex: 1;
            padding: 15px;
            background: #f9f9f9;
            border-radius: 5px;
            margin: 0 10px;
        }
        .info-box:first-child {
            margin-left: 0;
        }
        .info-box:last-child {
            margin-right: 0;
        }
        .info-box h3 {
            margin-top: 0;
            color: #2c3e50;
            border-bottom: 1px solid #eee;
            padding-bottom: 5px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 10px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
            font-weight: bold;
        }
        .total-section {
            text-align: right;
            margin-top: 20px;
            font-size: 1.1em;
        }
        .status {
            display: inline-block;
            padding: 5px 10px;
            border-radius: 20px;
            font-weight: bold;
            text-transform: uppercase;
            font-size: 0.8em;
        }
        .status-pending { background-color: #f39c12; color: white; }
        .status-processing { background-color: #3498db; color: white; }
        .status-shipped { background-color: #9b59b6; color: white; }
        .status-delivered { background-color: #2ecc71; color: white; }
        .status-cancelled { background-color: #e74c3c; color: white; }
        .footer {
            margin-top: 50px;
            text-align: center;
            font-size: 0.9em;
            color: #7f8c8d;
            border-top: 1px solid #eee;
            padding-top: 20px;
        }
        @media print {
            .no-print {
                display: none !important;
            }
            body {
                padding: 0;
            }
            .header {
                border: none;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>FACTURE #{{ order.id }}</h1>
        <p>Date: {{ order.createdAt|date('d/m/Y H:i') }}</p>
        <span class="status status-{{ order.status }}">
            {{ ('order.status.' ~ order.status)|trans }}
        </span>
    </div>

    <div class="info-section">
        <div class="info-box">
            <h3>Informations client</h3>
            <p><strong>Email:</strong> {{ order.user.email }}</p>
            {% if order.phoneNumber is not empty %}
                <p><strong>Téléphone:</strong> {{ order.phoneNumber }}</p>
            {% endif %}
        </div>
        <div class="info-box">
            <h3>Adresse de livraison</h3>
            <p>{{ order.shippingAddress|nl2br }}</p>
        </div>
    </div>

    <h3>Détails de la commande</h3>
    <table>
        <thead>
            <tr>
                <th>Produit</th>
                <th>Prix unitaire</th>
                <th>Quantité</th>
                <th>Total</th>
            </tr>
        </thead>
        <tbody>
            {% for item in order.items %}
            <tr>
                <td>{{ item.product.name|default('Produit inconnu') }}</td>
                <td>{{ item.price|number_format(2, ',', ' ') }} TND</td>
                <td>{{ item.quantity }}</td>
                <td>{{ (item.price * item.quantity)|number_format(2, ',', ' ') }} TND</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="total-section">
        <p><strong>Total TTC:</strong> {{ order.total|number_format(2, ',', ' ') }} TND</p>
    </div>

    <div class="footer">
        <p>Merci pour votre confiance !</p>
        <p>Urbanspace - Tél: +216 XX XXX XXX - Email: contact@urbanspace.tn</p>
        <p>Date d'impression: {{ 'now'|date('d/m/Y H:i') }}</p>
    </div>

    <div class="no-print" style="text-align: center; margin-top: 30px;">
        <button onclick="window.print()" style="padding: 10px 20px; background: #3498db; color: white; border: none; border-radius: 4px; cursor: pointer; font-size: 16px;">
            Imprimer la facture
        </button>
        <button onclick="window.close()" style="padding: 10px 20px; margin-left: 10px; background: #95a5a6; color: white; border: none; border-radius: 4px; cursor: pointer; font-size: 16px;">
            Fermer
        </button>
    </div>

    <script>
        // Auto-print when page loads if opened in new window
        if (window.opener) {
            window.focus();
            setTimeout(function() {
                window.print();
            }, 500);
        }
    </script>
</body>
</html>
